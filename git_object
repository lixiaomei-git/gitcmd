块 blob
	一个blob保存一个文件的数据，不包含文件的元数据 
目录树 tree
	一个目录树代表一层目录信息，包括blob标志符，路径，文件的元数据，也可以递归引用其他目录树
提交 commit
	一个提交保存版本库中每一次变化的元数据，每一个提交对象指向一个目录树，这个目录树对象在一张完整的快照中捕获提交时版本库的状态
标签 tag
	一个标签对象分配一个任意的可读名字给一个特定对象，通常是一个提交对象
